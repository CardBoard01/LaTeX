\documentclass[dvipdfmx, titlepage]{jsarticle}

\usepackage{bm}
\usepackage{amsmath}
\usepackage{amsfonts}		% 白抜き文字 \mathbb{} に必要
\usepackage{amsthm}		% \qedhereに必要?
\usepackage{amssymb}		% \blacksquareに必要
\usepackage{exscale}		% \small \largeのときに和記号、積分記号のバランスをよくする
\usepackage{cases}		% \numcases に必要
\usepackage{here}
\usepackage{empheq}		% \empheq
\usepackage{listings}		% ソースコード
\usepackage{moreverb}		% ソースコード挿入\listinginputに必要
\usepackage{framed}
\usepackage{color}			% \textcolor用
\usepackage{tabularx}		% \tabularx環境
\usepackage[dvipdfmx]{graphicx}
\usepackage{arydshln}
\usepackage{fancyhdr}		% pagestyle{fancy}
\usepackage{hangcaption}	% \hangcaption用
\usepackage{cancel}
\usepackage{txfonts}		% :=  \coloneqq \eqqcolon

\usepackage{docmute}

\usepackage[dvipdfmx]{hyperref, color}  % \hyperref用
\usepackage{pxjahyper}		% ハイパーリンク
\hypersetup{
  bookmarks = true,			% しおりを作る
  bookmarksnumbered = true,	% しおりのラベル
  bookmarksopen = true,		% ブックマークを開く
  colorlinks = false,				% カラーリンク
  linkcolor = red,				% 内部参照リンク用のカラー
  citecolor = green,				% 文献参照用リンク用のカラー 
  pdfborder={0 0 0},			% 枠なし
  bookmarkstype=toc
}

\captionwidth=120mm			% hangcaptionのサイズを指定

% \pagestyle{fancy}
% \lhead{}
% \rhead{\today}
% \cfoot{}
% \rfoot{}

\setlength{\headheight}{5mm}		%ヘッダの高さを0にする
\setlength{\topmargin}{-10.4truemm}	%上マージンを変える
\addtolength{\textheight}{20truemm}	%下の余白を変更

\setcounter{tocdepth}{2}		% 目次出力をsubsectionまで書く

\newcommand{\divergence}[0]{\mathrm{div}}	% div
\newcommand{\grad}[0]{\mathrm{grad}}		% grad
\newcommand{\rot}[0]{\mathrm{rot}}			% rot
\newcommand{\diff}[0]{\mathrm{d}}			% 微分記号

\newcommand{\6}[0]{\partial}		% 偏微分記号もだるいので\6にする
\newcommand{\dsum}[0]{\displaystyle \sum}
\newcommand{\tsum}[0]{\textstyle \sum}

% d/dx, d^n/dx^n の出力
\newcommand{\dx}[1] {
    \frac{d^{#1}}{dx^{#1}}
}

% d/dz, d^n/dz^n の出力
\newcommand{\dz}[1] {
    \frac{d^{#1}}{dz^{#1}}
}

\renewcommand{\(}[0]{\left(}
\renewcommand{\)}[0]{\right)}
\renewcommand{\[}[0]{\left[}
\renewcommand{\]}[0]{\right]}




% ∵描画関数\sinceの作成
\def\since{%
\setlength{\unitlength}{.8pt}%
\thinlines %
\begin{picture}(12,12)%
\put(0 ,6){.}
\put(3,0){.}
\put(6,6){.}
\end{picture}%
}%

% 平行記号
\def\heiko{\mathrel{\raise.3ex\hbox{\scalebox{.7}{%
    \rotatebox[origin=c]{-7}{/}%
    \kern-.35em\rotatebox[origin=c]{-7}{/}}}}}%
\def\notheiko{\mathrel{\raise.3ex\hbox{\scalebox{.7}{%
    \ooalign{\rotatebox[origin=c]{-7}{/}\kern-.35em%
             \rotatebox[origin=c]{-7}{/}\crcr%
               \hfil$\backslash$\hfil}}}}}%

% 太線
\makeatletter
\def\Hline{%
\noalign{\ifnum0=`}\fi\hrule \@height 1pt \futurelet
\reserved@a\@xhline}
\makeatother


% ソースコードの書き方設定
\lstset{
    basicstyle={\ttfamily\small}, 	% 書体の指定
    % frame=tRBl, 		% フレームの指定
    % framesep=10pt, 	% フレームと中身（コード）の間隔
    breaklines=true,		% 行が長くなった場合の改行
    linewidth=12cm, 	% フレームの横幅
    lineskip=-0.5ex, 		% 行間の調整
    tabsize=2 			% Tabを何文字幅にするかの指定
}


\makeatletter
%% オリジナルの \paragraph の定義には手をつけずプリアンブルで再定義する
%%
\if@twocolumn
  \renewcommand{\paragraph}{\@startsection{paragraph}{4}{\z@}%
    {\z@}{-1zw}% 改行せず 1zw のアキ
    {\normalfont\normalsize\headfont}}
\else
  \renewcommand{\paragraph}{\@startsection{paragraph}{4}{\z@}%
    {0.5\Cvs \@plus.5\Cdp \@minus.2\Cdp}%
    {-1zw}% 改行せず 1zw のアキ
    {\normalfont\normalsize\headfont}}
\fi
\makeatother

% jsbook.clsにおける概要のレイアウト設定
% \makeatletter
% \renewenvironment{abstract}{%
%   \titlepage
%   \null\vfil
%   \@beginparpenalty\@lowpenalty
%   \begin{center}%
%     {\bfseries\abstractname}%
%     \@endparpenalty\@M
%   \end{center}}%
%   {\par\vfil\null\endtitlepage}
% \newcommand{\abstractname}{概要}
% \makeatother


\title{}
\author{}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}

\end{abstract}

\tableofcontents




\begin{thebibliography}{99}
  \bibitem{}
\end{thebibliography}

\end{document}